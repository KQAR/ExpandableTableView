import XCTest
@testable import ExpandableTableView

final class ExpandableTableViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExpandableTableView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
